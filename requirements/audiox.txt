# Optional dependencies for AudioXPipeline (HKUSTAudio/AudioX* weights).
#
# Install (from repo root, after vllm + editable vllm-omni):
#   pip install -r requirements/audiox.txt
#   pip install -r requirements/audiox-protobuf.txt
#
# Or use the PyPI extra (same two steps for protobuf — see audiox-protobuf.txt):
#   pip install -e ".[audiox]"
#   pip install -r requirements/audiox-protobuf.txt
#
# descript-audio-codec pulls descript-audiotools, which pins protobuf<3.20; vLLM 0.18+
# requires protobuf>=5.29.6. The second file re-pins protobuf after DAC installs.

# Video-conditioned tasks (v2a, tv2a, …) require decord for frame loading.
decord>=0.6.0
k-diffusion>=0.1.1.post1
einops-exts>=0.0.4
alias-free-torch>=0.0.6
vector-quantize-pytorch>=1.0.0
# clip (via k-diffusion) imports pkg_resources; setuptools>=81 removed it
setuptools>=77.0.3,<81.0.0
descript-audio-codec>=1.0.0
